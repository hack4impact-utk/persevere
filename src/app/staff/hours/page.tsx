import { type ReactElement } from "react";

import HoursReviewList from "@/components/staff/hours/hours-review-list";

/** Staff hours review page. */
export default function HoursPage(): ReactElement {
  return <HoursReviewList />;
}
