import { type JSX } from "react";

import ApprovalsPage from "@/components/staff/approvals/approvals-page";

/** Approvals hub for hours, RSVPs, and attendance. */
export default function StaffApprovalsPage(): JSX.Element {
  return <ApprovalsPage />;
}
