import { type ReactElement } from "react";

import SkillsSettingsClient from "./skills-settings-client";

/** Skills & Interests catalog management. Auth handled by settings layout. */
export default function SkillsSettingsPage(): ReactElement {
  return <SkillsSettingsClient />;
}
